<font color="white">

  <h4>Hello Visitor! Create an Account!</h4>

  <%= render partial: '/errors', locals: { record: @user } %>

  <%= form_for @user, url: users_path, method: :post do |w| %>

    <p>
      <div><%= w.label :name %></div>
      <div><%= w.text_field :name %></div>
    </p>
</font>

  <%= render 'form' %>
  <% end %>